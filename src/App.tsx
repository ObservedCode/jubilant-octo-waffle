import React from 'react';
import styled from 'styled-components';

const AppContainer = styled.div`
 width: 100%;
 height: 100%;
 display: flex;
 flex-directionL: column;
 alight-items: center;
`
function App() {
  // fetch api 
  // from graphql
  return (
    <AppContainer className="App">hello </AppContainer>
  );
}

export default App;
